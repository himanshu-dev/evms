@model IEnumerable<MvcApplication1.Models.Voucher>

    @{
        ViewBag.Title = "Index";
        Layout = "~/Views/Shared/_EMPLayout.cshtml";
        var Vouchers = @ViewBag.Vouchers;
    }

    <table class="table table-hover">
        <thead>
            <tr>
                <th>
                    Employee ID
                </th>
                <th>
                    Voucher ID
                </th>
                <th>
                    Voucher Type
                </th>
                <th>
                    Submission Date
                </th>
                <th>
                    Status(FSO)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Vouchers)
            {
                <tr>
                    <td>
                        #00-@item.EmployeeID
                    </td>
                    <td>
                        #00-@item.VoucherID
                    </td>
                    <td>
                        @item.VoucherType
                    </td>
                    <td>
                        @item.SubmitDate
                    </td>
                    <td>
                        <b style="margin-left:2px;">
                            @item.StatusByFSO
                        </b>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    @section childLayout{
        @Html.Partial("_childLayoutFsoUnseen")
    }
